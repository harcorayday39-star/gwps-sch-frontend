import React,{useState} from 'react';import { load, save } from '../utils/storage';export default function AdminQuestions(){const [title,setTitle]=useState('New Exam');const db=load();function create(){const data={id:'EX'+(db.exams.length+1),title,durationMinutes:5,mcqs:[],written:[]};db.exams.push(data);save(db);alert('Created exam');}return (<div className='container'><div className='card'><h3>Upload Questions</h3><div><label>Title</label><input value={title} onChange={e=>setTitle(e.target.value)} /></div><button style={{marginTop:8}} onClick={create}>Create Exam</button></div></div>); }