import React,{useState} from 'react';import { useNavigate } from 'react-router-dom';import { load } from '../utils/storage';export default function StudentLogin(){const [reg,setReg]=useState('');const [pw,setPw]=useState('');const nav=useNavigate();function submit(e){e.preventDefault();const db=load();const s=db.students.find(x=>x.reg===reg&&x.password===pw);if(s){localStorage.setItem('gwpss_user',JSON.stringify({type:'student',reg}));nav('/student/dashboard');}else alert('Invalid credentials (try GWPS001 / 12345)');}return (<div className='container'><div className='card'><h3>Student Login</h3><form onSubmit={submit}><div><label>Reg No</label><br/><input value={reg} onChange={e=>setReg(e.target.value)} /></div><div style={{marginTop:8}}><label>Password</label><br/><input type='password' value={pw} onChange={e=>setPw(e.target.value)} /></div><button style={{marginTop:12}}>Login</button></form></div></div>); }