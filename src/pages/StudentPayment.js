import React from 'react';import { load } from '../utils/storage';export default function StudentPayment(){const db=load();const p=db.paymentInfo;return (<div className='container'><div className='card'><h3>Payment Information</h3><p>Account Name: {p.accountName}</p><p>Account Number: {p.accountNumber}</p><p>Bank: {p.bank}</p><p>Note: Payments are offline. Upload receipt to management after payment.</p></div></div>); }