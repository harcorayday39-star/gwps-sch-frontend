import React,{useState} from 'react';import { useNavigate } from 'react-router-dom';import { load } from '../utils/storage';export default function AdminLogin(){const [u,setU]=useState('');const [p,setP]=useState('');const nav=useNavigate();function submit(e){e.preventDefault();const db=load();if(u===db.admin.username && p===db.admin.password){localStorage.setItem('gwpss_user',JSON.stringify({type:'admin'}));nav('/admin/dashboard');}else alert('Invalid admin (try admin/admin123)');}return (<div className='container'><div className='card'><h3>Management Login</h3><form onSubmit={submit}><div><label>Username</label><br/><input value={u} onChange={e=>setU(e.target.value)} /></div><div style={{marginTop:8}}><label>Password</label><br/><input type='password' value={p} onChange={e=>setP(e.target.value)} /></div><button style={{marginTop:12}}>Login</button></form></div></div>); }